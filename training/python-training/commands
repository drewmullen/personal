# python commands ive learned

Number Game Intro
import <python standard library>

#generates a # between 1 and 10
random.randint(1,10)

#makes a string into a list
list(<string>)

#add a single item to a list
mylist.append(<item>)

#add multiple items to a list
mylist.extend(<[1,2,3]>) or ("me" * 5)

#split a string by spaces (or separators)
string.split() or string.split(', ')

#turn a list into a string ex: comma separated
', '.join(<list>)

#list / string indexs
string.index('<>') ex: string = 'abc'; string.index('a') returns 0

#returns True if var is and alpha
str.isalpha()

#def clear():
#    if os.name == 'nt':
#        os.system('cls')
#    else:
#        os.system('clear')
