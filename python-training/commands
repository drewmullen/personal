# python commands ive learned

Number Game Intro
import <python standard library>

random.randint(1,10) #generates a # between 1 and 10

while True:
  guess = int(input("guess a number between 1 and 10: "))

  if guess == secret_num:
